# Get a Table of Available Ranks

Get the available ranks as a data frame. The ranks are returned in the
correct hierarchical order from highest to lowest. Other ranks may be
used, but only those listed here will be represented by coloured nodes
in the visualisation.

## Usage

``` r
available_ranks()
```

## Value

a tibble with columns `level` and `de` (ranks in German).
